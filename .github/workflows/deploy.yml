name: Deploy homelab

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          whoami
          id
          ls -la /srv/homelab/.git | head
          /srv/homelab/deploy.sh
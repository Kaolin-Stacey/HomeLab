diff --git a/.github/workflows/homelab-control.yml b/.github/workflows/homelab-control.yml
index a61898e..540bc9e 100644
--- a/.github/workflows/homelab-control.yml
+++ b/.github/workflows/homelab-control.yml
@@ -1,64 +1,64 @@
-name: Homelab Control
-
-on:
-  workflow_dispatch:
-    inputs:
-      stack:
-        description: "Stack path, example: media/automation"
-        required: true
-        default: "media/automation"
-
-      all_stacks:
-        description: "Run against all non-core stacks"
-        required: true
-        default: "false"
-        type: choice
-        options:
-          - "false"
-          - "true"
-
-      action:
-        description: "Compose action"
-        required: true
-        default: "up"
-        type: choice
-        options:
-          - up
-          - down
-          - restart
-          - pull
-          - logs
-
-      allow_core:
-        description: "Allow core stacks"
-        required: true
-        default: "false"
-        type: choice
-        options:
-          - "false"
-          - "true"
-
-jobs:
-  control:
-    runs-on: self-hosted
-
-    steps:
-      - name: Update repo and sync runtime
-        run: |
-          /srv/homelab-repo/scripts/update-repo.sh
-
-      - name: Run compose action
-        env:
-          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
-          OP_VAULT_NAME: "HomeLab"
-          OP_ITEM_NAME: "Docker Compose Secrets"
-          ALLOW_CORE: ${{ github.event.inputs.allow_core }}
-        run: |
-          if [[ "${{ github.event.inputs.all_stacks }}" == "true" ]]; then
-            /srv/homelab/scripts/compose-all.sh \
-              "${{ github.event.inputs.action }}"
-          else
-            /srv/homelab/scripts/compose-stack.sh \
-              "${{ github.event.inputs.stack }}" \
-              "${{ github.event.inputs.action }}"
+name: Homelab Control
+
+on:
+  workflow_dispatch:
+    inputs:
+      stack:
+        description: "Stack path, example: media/automation"
+        required: true
+        default: "media/automation"
+
+      all_stacks:
+        description: "Run against all non-core stacks"
+        required: true
+        default: "false"
+        type: choice
+        options:
+          - "false"
+          - "true"
+
+      action:
+        description: "Compose action"
+        required: true
+        default: "up"
+        type: choice
+        options:
+          - up
+          - down
+          - restart
+          - pull
+          - logs
+
+      allow_core:
+        description: "Allow core stacks"
+        required: true
+        default: "false"
+        type: choice
+        options:
+          - "false"
+          - "true"
+
+jobs:
+  control:
+    runs-on: self-hosted
+
+    steps:
+      - name: Update repo and sync runtime
+        run: |
+          /srv/homelab-repo/scripts/update-repo.sh
+
+      - name: Run compose action
+        env:
+          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
+          OP_VAULT_NAME: "HomeLab"
+          OP_ITEM_NAME: "Docker Compose Secrets"
+          ALLOW_CORE: ${{ github.event.inputs.allow_core }}
+        run: |
+          if [[ "${{ github.event.inputs.all_stacks }}" == "true" ]]; then
+            /srv/homelab/scripts/compose-all.sh \
+              "${{ github.event.inputs.action }}"
+          else
+            /srv/homelab/scripts/compose-stack.sh \
+              "${{ github.event.inputs.stack }}" \
+              "${{ github.event.inputs.action }}"
           fi
\ No newline at end of file
diff --git a/.gitignore b/.gitignore
index 7a3ec1a..c7f4b5e 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,25 +1,25 @@
-# Environment files
-.env
-.env.*
-**/.env
-
-# Docker app config / state folders
-**/data/
-**/config/
-**/logs/
-
-# Let’s Encrypt / reverse proxy certs
-**/letsencrypt/
-**/acme.json
-
-# Database files
-**/mysql/
-**/*.db
-
-# Media & downloads
-/media/
-/downloads/
-
-# OS junk
-.DS_Store
+# Environment files
+.env
+.env.*
+**/.env
+
+# Docker app config / state folders
+**/data/
+**/config/
+**/logs/
+
+# Let’s Encrypt / reverse proxy certs
+**/letsencrypt/
+**/acme.json
+
+# Database files
+**/mysql/
+**/*.db
+
+# Media & downloads
+/media/
+/downloads/
+
+# OS junk
+.DS_Store
 Thumbs.db
\ No newline at end of file
diff --git a/README.md b/README.md
index 2443d42..db52076 100644
--- a/README.md
+++ b/README.md
@@ -1,79 +1,79 @@
-# Self-Hosted Homelab Infrastructure
-A modular, self-hosted homelab built with Docker Compose, focused on automation, media pipelines, networking, and smart home integration.
-
-This project serves as a hands-on environment to explore DevOps practices, containerised infrastructure, and real-world system design.
-
-Designed to simulate a production-like environment for deploying and managing interconnected services using modern DevOps practices.
-
----
-
-## Overview
-
-The homelab is organised into independent stacks, each responsible for a specific domain (media, networking, development, monitoring, etc.).  
-
-This modular architecture improves maintainability, scalability, and isolation between services while allowing individual components to be developed and deployed independently.
-
----
-
-## Key Features
-- Modular stack-based architecture for service isolation and scalability  
-- Automated media pipeline with integrated downloaders and indexers  
-- Reverse proxy with SSL for secure service exposure  
-- Centralised CI/CD workflow using GitHub Actions and a self-hosted runner  
-- Secure secret management with no credentials stored in the repository  
-- Self-hosted development and monitoring environments  
----
-
-## Purpose
-
-This homelab is used to:
-- experiment with DevOps and infrastructure design  
-- build and manage containerised systems  
-- explore automation and orchestration  
-- simulate real-world deployment environments  
----
-
-## Project Structure
-
-```bash
-HomeLab/
-├─ stacks/
-│  ├─ database/        # MySQL
-│  ├─ dev/             # Containerised development environment (Ubuntu)
-│  ├─ downloaders/     # Transmission, Deluge
-│  ├─ games/           # Crafty (Minecraft server management)
-│  ├─ home_assistant/  # Home Assistant
-│  ├─ media/           # Sonarr, Radarr, Prowlarr, FlareSolverr
-│  ├─ metrics/         # Glances (monitoring)
-│  ├─ networking/      # Nginx Proxy Manager, Pi-hole
-│  ├─ plex/            # Plex media server
-│  └─ voice/           # Murmur (Mumble server)
-└─ README.md
-````
----
-
-## CI/CD & Secrets Management
-
-- Implemented CI/CD-style deployments using GitHub Actions with a self-hosted runner
-
-- Managed sensitive configuration using GitHub Secrets, securely injecting environment variables at runtime
-
-- Automated stack deployments via GitHub Actions triggering `docker compose up`
-
-- Ensured no credentials or sensitive data are stored in the repository
-  
----
-
-## Tech Stack
-
-- Docker / Docker Compose
-
-- Linux (self-hosted environment)
-
-- Networking (reverse proxy, DNS filtering, segmentation)
-
-- Git & GitHub Actions (CI/CD workflows)
-
-- Secrets management (GitHub Secrets)
-
+# Self-Hosted Homelab Infrastructure
+A modular, self-hosted homelab built with Docker Compose, focused on automation, media pipelines, networking, and smart home integration.
+
+This project serves as a hands-on environment to explore DevOps practices, containerised infrastructure, and real-world system design.
+
+Designed to simulate a production-like environment for deploying and managing interconnected services using modern DevOps practices.
+
+---
+
+## Overview
+
+The homelab is organised into independent stacks, each responsible for a specific domain (media, networking, development, monitoring, etc.).  
+
+This modular architecture improves maintainability, scalability, and isolation between services while allowing individual components to be developed and deployed independently.
+
+---
+
+## Key Features
+- Modular stack-based architecture for service isolation and scalability  
+- Automated media pipeline with integrated downloaders and indexers  
+- Reverse proxy with SSL for secure service exposure  
+- Centralised CI/CD workflow using GitHub Actions and a self-hosted runner  
+- Secure secret management with no credentials stored in the repository  
+- Self-hosted development and monitoring environments  
+---
+
+## Purpose
+
+This homelab is used to:
+- experiment with DevOps and infrastructure design  
+- build and manage containerised systems  
+- explore automation and orchestration  
+- simulate real-world deployment environments  
+---
+
+## Project Structure
+
+```bash
+HomeLab/
+├─ stacks/
+│  ├─ database/        # MySQL
+│  ├─ dev/             # Containerised development environment (Ubuntu)
+│  ├─ downloaders/     # Transmission, Deluge
+│  ├─ games/           # Crafty (Minecraft server management)
+│  ├─ home_assistant/  # Home Assistant
+│  ├─ media/           # Sonarr, Radarr, Prowlarr, FlareSolverr
+│  ├─ metrics/         # Glances (monitoring)
+│  ├─ networking/      # Nginx Proxy Manager, Pi-hole
+│  ├─ plex/            # Plex media server
+│  └─ voice/           # Murmur (Mumble server)
+└─ README.md
+````
+---
+
+## CI/CD & Secrets Management
+
+- Implemented CI/CD-style deployments using GitHub Actions with a self-hosted runner
+
+- Managed sensitive configuration using GitHub Secrets, securely injecting environment variables at runtime
+
+- Automated stack deployments via GitHub Actions triggering `docker compose up`
+
+- Ensured no credentials or sensitive data are stored in the repository
+  
+---
+
+## Tech Stack
+
+- Docker / Docker Compose
+
+- Linux (self-hosted environment)
+
+- Networking (reverse proxy, DNS filtering, segmentation)
+
+- Git & GitHub Actions (CI/CD workflows)
+
+- Secrets management (GitHub Secrets)
+
 - Monitoring and observability (Glances, future Grafana)
\ No newline at end of file
diff --git a/scripts/compose-all.sh b/scripts/compose-all.sh
old mode 100644
new mode 100755
diff --git a/scripts/compose-stack.sh b/scripts/compose-stack.sh
old mode 100644
new mode 100755
diff --git a/scripts/sync-runtime.sh b/scripts/sync-runtime.sh
old mode 100644
new mode 100755
diff --git a/scripts/update-repo.sh b/scripts/update-repo.sh
old mode 100644
new mode 100755
diff --git a/stacks/core/dns/docker-compose.yml b/stacks/core/dns/docker-compose.yml
index e5bd79a..8b56bdf 100644
--- a/stacks/core/dns/docker-compose.yml
+++ b/stacks/core/dns/docker-compose.yml
@@ -1,27 +1,27 @@
-name: pihole
-
-networks:
-  proxy_net:
-    external: true
-
-services:
-  pihole:
-    image: pihole/pihole:latest
-    container_name: pihole
-    restart: unless-stopped
-    ports:
-          - "53:53/tcp"
-          - "53:53/udp"
-          - "8082:80/tcp" # Pi-hole admin interface
-    environment:
-      TZ: ${TZ}
-      WEBPASSWORD: ${PIHOLE_WEBPASSWORD}
-      DNS1: 1.1.1.1
-      DNS2: 9.9.9.9
-    volumes:
-      - ${DOCKER_ROOT}/pihole/etc-pihole:/etc/pihole
-      - ${DOCKER_ROOT}/pihole/etc-dnsmasq.d:/etc/dnsmasq.d
-    cap_add:
-      - NET_ADMIN
-    networks:
+name: pihole
+
+networks:
+  proxy_net:
+    external: true
+
+services:
+  pihole:
+    image: pihole/pihole:latest
+    container_name: pihole
+    restart: unless-stopped
+    ports:
+          - "53:53/tcp"
+          - "53:53/udp"
+          - "8082:80/tcp" # Pi-hole admin interface
+    environment:
+      TZ: ${TZ}
+      WEBPASSWORD: ${PIHOLE_WEBPASSWORD}
+      DNS1: 1.1.1.1
+      DNS2: 9.9.9.9
+    volumes:
+      - ${DOCKER_ROOT}/pihole/etc-pihole:/etc/pihole
+      - ${DOCKER_ROOT}/pihole/etc-dnsmasq.d:/etc/dnsmasq.d
+    cap_add:
+      - NET_ADMIN
+    networks:
       - proxy_net
\ No newline at end of file
diff --git a/stacks/core/proxy/docker-compose.yml b/stacks/core/proxy/docker-compose.yml
index 93f3722..9da7e23 100644
--- a/stacks/core/proxy/docker-compose.yml
+++ b/stacks/core/proxy/docker-compose.yml
@@ -1,24 +1,24 @@
-name: npm
-
-networks:
-  proxy_net:
-    external: true
-
-services:
-  npm:
-    image: jc21/nginx-proxy-manager:latest
-    container_name: npm
-    restart: unless-stopped
-    ports:
-      - "80:80"
-      - "443:443"
-      - "81:81"   # NPM admin UI
-    environment:
-      - TZ=${TZ}
-    volumes:
-      - ${DOCKER_ROOT}/npm/data:/data
-      - ${DOCKER_ROOT}/npm/letsencrypt:/etc/letsencrypt
-    extra_hosts:
-      - "host.docker.internal:host-gateway"
-    networks:
+name: npm
+
+networks:
+  proxy_net:
+    external: true
+
+services:
+  npm:
+    image: jc21/nginx-proxy-manager:latest
+    container_name: npm
+    restart: unless-stopped
+    ports:
+      - "80:80"
+      - "443:443"
+      - "81:81"   # NPM admin UI
+    environment:
+      - TZ=${TZ}
+    volumes:
+      - ${DOCKER_ROOT}/npm/data:/data
+      - ${DOCKER_ROOT}/npm/letsencrypt:/etc/letsencrypt
+    extra_hosts:
+      - "host.docker.internal:host-gateway"
+    networks:
       - proxy_net
\ No newline at end of file
diff --git a/stacks/dev/ubuntu/Dockerfile b/stacks/dev/ubuntu/Dockerfile
index 938d7b9..17db41f 100644
--- a/stacks/dev/ubuntu/Dockerfile
+++ b/stacks/dev/ubuntu/Dockerfile
@@ -1,23 +1,23 @@
-FROM ubuntu:24.04
-
-ENV DEBIAN_FRONTEND=noninteractive
-
-COPY install-deps.sh /tmp/install-deps.sh
-RUN chmod +x /tmp/install-deps.sh && /tmp/install-deps.sh
-
-RUN mkdir /var/run/sshd
-
-ARG USERNAME=kaolin
-ARG PASSWORD=kaolin
-
-RUN useradd -m -s /bin/bash ${USERNAME} \
-    && echo "${USERNAME}:${PASSWORD}" | chpasswd \
-    && usermod -aG sudo ${USERNAME}
-
-COPY sshd_config /etc/ssh/sshd_config
-
-RUN mkdir -p /workspace \
-    && chown -R ${USERNAME}:${USERNAME} /workspace
-
-EXPOSE 22
+FROM ubuntu:24.04
+
+ENV DEBIAN_FRONTEND=noninteractive
+
+COPY install-deps.sh /tmp/install-deps.sh
+RUN chmod +x /tmp/install-deps.sh && /tmp/install-deps.sh
+
+RUN mkdir /var/run/sshd
+
+ARG USERNAME=kaolin
+ARG PASSWORD=kaolin
+
+RUN useradd -m -s /bin/bash ${USERNAME} \
+    && echo "${USERNAME}:${PASSWORD}" | chpasswd \
+    && usermod -aG sudo ${USERNAME}
+
+COPY sshd_config /etc/ssh/sshd_config
+
+RUN mkdir -p /workspace \
+    && chown -R ${USERNAME}:${USERNAME} /workspace
+
+EXPOSE 22
 CMD ["/usr/sbin/sshd", "-D"]
\ No newline at end of file
diff --git a/stacks/dev/ubuntu/docker-compose.yml b/stacks/dev/ubuntu/docker-compose.yml
index 4cf185f..9a478bd 100644
--- a/stacks/dev/ubuntu/docker-compose.yml
+++ b/stacks/dev/ubuntu/docker-compose.yml
@@ -1,19 +1,19 @@
-name: ubuntu-dev
-services:
-  ubuntu-dev:
-    build:
-      context: .
-      args:
-        USERNAME: ${UBUNTU-DEV-USERNAME}
-        PASSWORD: ${UBUNTU-DEV-PASSWORD}
-    container_name: ubuntu-dev
-    hostname: ubuntu-dev
-    restart: unless-stopped
-    tty: true
-    stdin_open: true
-    ports:
-      - "22222:22"
-    volumes:
-      - ${DOCKER_ROOT}/ubuntu:/workspace
-    environment:
+name: ubuntu-dev
+services:
+  ubuntu-dev:
+    build:
+      context: .
+      args:
+        USERNAME: ${UBUNTU-DEV-USERNAME}
+        PASSWORD: ${UBUNTU-DEV-PASSWORD}
+    container_name: ubuntu-dev
+    hostname: ubuntu-dev
+    restart: unless-stopped
+    tty: true
+    stdin_open: true
+    ports:
+      - "22222:22"
+    volumes:
+      - ${DOCKER_ROOT}/ubuntu:/workspace
+    environment:
       TZ: ${TZ}
\ No newline at end of file
diff --git a/stacks/dev/ubuntu/install-deps.sh b/stacks/dev/ubuntu/install-deps.sh
index 1ec7da3..f308f76 100644
--- a/stacks/dev/ubuntu/install-deps.sh
+++ b/stacks/dev/ubuntu/install-deps.sh
@@ -1,29 +1,29 @@
-#!/usr/bin/env bash
-set -e
-
-apt-get update
-apt-get install -y \
-    openssh-server \
-    sudo \
-    git \
-    curl \
-    wget \
-    nano \
-    vim \
-    build-essential \
-    gdb \
-    valgrind \
-    ca-certificates \
-    python3 \
-    python3-pip \
-    python3-venv \
-    python3-dev \
-    python-is-python3 \
-    openjdk-17-jdk
-
-curl -O https://download.clojure.org/install/linux-install-1.11.1.1413.sh
-chmod +x linux-install-1.11.1.1413.sh
-./linux-install-1.11.1.1413.sh
-rm linux-install-1.11.1.1413.sh
-
+#!/usr/bin/env bash
+set -e
+
+apt-get update
+apt-get install -y \
+    openssh-server \
+    sudo \
+    git \
+    curl \
+    wget \
+    nano \
+    vim \
+    build-essential \
+    gdb \
+    valgrind \
+    ca-certificates \
+    python3 \
+    python3-pip \
+    python3-venv \
+    python3-dev \
+    python-is-python3 \
+    openjdk-17-jdk
+
+curl -O https://download.clojure.org/install/linux-install-1.11.1.1413.sh
+chmod +x linux-install-1.11.1.1413.sh
+./linux-install-1.11.1.1413.sh
+rm linux-install-1.11.1.1413.sh
+
 rm -rf /var/lib/apt/lists/*
\ No newline at end of file
diff --git a/stacks/dev/ubuntu/sshd_config b/stacks/dev/ubuntu/sshd_config
index b7f322f..5180357 100644
--- a/stacks/dev/ubuntu/sshd_config
+++ b/stacks/dev/ubuntu/sshd_config
@@ -1,9 +1,9 @@
-Port 22
-Protocol 2
-PermitRootLogin no
-PasswordAuthentication yes
-PubkeyAuthentication yes
-X11Forwarding no
-UsePAM yes
-PrintMotd no
+Port 22
+Protocol 2
+PermitRootLogin no
+PasswordAuthentication yes
+PubkeyAuthentication yes
+X11Forwarding no
+UsePAM yes
+PrintMotd no
 Subsystem sftp /usr/lib/openssh/sftp-server
\ No newline at end of file
diff --git a/stacks/games/minecraft/docker-compose.yml b/stacks/games/minecraft/docker-compose.yml
index b1384c2..4698570 100644
--- a/stacks/games/minecraft/docker-compose.yml
+++ b/stacks/games/minecraft/docker-compose.yml
@@ -1,27 +1,27 @@
-name: minecraft
-
-version: '3'
-networks:
-  proxy_net:
-    external: true
-services:
-  crafty:
-    container_name: crafty
-    image: registry.gitlab.com/crafty-controller/crafty-4:latest
-    restart: always
-    environment:
-        - TZ=${TZ}
-        - JVM_OPTS="-Dio.netty.leakDetection.level=DISABLED"
-    ports:
-    - "8443:8443"           # HTTPS (tcp)
-    - "8125:8123"           # Dynmap (tcp)
-    - "25560-25570:25560-25570/tcp"
-    - "25560-25570:25560-25570/udp"
-    volumes:
-        - ${DOCKER_ROOT}/minecraft/backups:/crafty/backups
-        - ${DOCKER_ROOT}/minecraft/logs:/crafty/logs
-        - ${DOCKER_ROOT}/minecraft/servers:/crafty/servers
-        - ${DOCKER_ROOT}/minecraft/config:/crafty/app/config
-        - ${DOCKER_ROOT}/minecraft/import:/crafty/import
-    networks:
+name: minecraft
+
+version: '3'
+networks:
+  proxy_net:
+    external: true
+services:
+  crafty:
+    container_name: crafty
+    image: registry.gitlab.com/crafty-controller/crafty-4:latest
+    restart: always
+    environment:
+        - TZ=${TZ}
+        - JVM_OPTS="-Dio.netty.leakDetection.level=DISABLED"
+    ports:
+    - "8443:8443"           # HTTPS (tcp)
+    - "8125:8123"           # Dynmap (tcp)
+    - "25560-25570:25560-25570/tcp"
+    - "25560-25570:25560-25570/udp"
+    volumes:
+        - ${DOCKER_ROOT}/minecraft/backups:/crafty/backups
+        - ${DOCKER_ROOT}/minecraft/logs:/crafty/logs
+        - ${DOCKER_ROOT}/minecraft/servers:/crafty/servers
+        - ${DOCKER_ROOT}/minecraft/config:/crafty/app/config
+        - ${DOCKER_ROOT}/minecraft/import:/crafty/import
+    networks:
         - proxy_net
\ No newline at end of file
diff --git a/stacks/home/home_assistant/docker-compose.yml b/stacks/home/home_assistant/docker-compose.yml
index bef875a..cf8cf5b 100644
--- a/stacks/home/home_assistant/docker-compose.yml
+++ b/stacks/home/home_assistant/docker-compose.yml
@@ -1,24 +1,24 @@
-name: home_assistant
-
-networks:
-  proxy_net:
-    external: true
-
-services:
-  homeassistant:
-    container_name: homeassistant
-    image: ghcr.io/home-assistant/home-assistant:stable
-    restart: unless-stopped
-    privileged: true
-    environment:
-      - TZ=${TZ}
-    volumes:
-      - ${DOCKER_ROOT}/homeassistant:/config
-      - /etc/localtime:/etc/localtime:ro
-      - /run/dbus:/run/dbus:ro
-      - /var/run/docker.sock:/var/run/docker.sock:ro
-    cap_add:
-      - NET_ADMIN
-      - NET_RAW
-    networks:
+name: home_assistant
+
+networks:
+  proxy_net:
+    external: true
+
+services:
+  homeassistant:
+    container_name: homeassistant
+    image: ghcr.io/home-assistant/home-assistant:stable
+    restart: unless-stopped
+    privileged: true
+    environment:
+      - TZ=${TZ}
+    volumes:
+      - ${DOCKER_ROOT}/homeassistant:/config
+      - /etc/localtime:/etc/localtime:ro
+      - /run/dbus:/run/dbus:ro
+      - /var/run/docker.sock:/var/run/docker.sock:ro
+    cap_add:
+      - NET_ADMIN
+      - NET_RAW
+    networks:
       - proxy_net
\ No newline at end of file
diff --git a/stacks/infrastructure/database/docker-compose.yml b/stacks/infrastructure/database/docker-compose.yml
index bc7f48c..cf76e10 100644
--- a/stacks/infrastructure/database/docker-compose.yml
+++ b/stacks/infrastructure/database/docker-compose.yml
@@ -1,25 +1,25 @@
-name: database
-
-services:
-  mysql:
-    image: mysql:latest
-    container_name: mysql
-    restart: always
-    ports:
-      - "3306:3306"
-    volumes:
-      - database:/var/lib/mysql
-    environment:
-      - MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD
-      - MYSQL_DATABASE=$MYSQL_DATABASE
-      - MYSQL_USER=$MYSQL_USER
-      - MYSQL_PASSWORD=$MYSQL_PASSWORD
-    networks:
-      - database
-
-volumes:
-  database:
-    external: true
-networks:
-  database:
+name: database
+
+services:
+  mysql:
+    image: mysql:latest
+    container_name: mysql
+    restart: always
+    ports:
+      - "3306:3306"
+    volumes:
+      - database:/var/lib/mysql
+    environment:
+      - MYSQL_ROOT_PASSWORD=$MYSQL_ROOT_PASSWORD
+      - MYSQL_DATABASE=$MYSQL_DATABASE
+      - MYSQL_USER=$MYSQL_USER
+      - MYSQL_PASSWORD=$MYSQL_PASSWORD
+    networks:
+      - database
+
+volumes:
+  database:
+    external: true
+networks:
+  database:
     external: true
\ No newline at end of file
diff --git a/stacks/infrastructure/metrics/docker-compose.yml b/stacks/infrastructure/metrics/docker-compose.yml
index 0f4afd7..3750de2 100644
--- a/stacks/infrastructure/metrics/docker-compose.yml
+++ b/stacks/infrastructure/metrics/docker-compose.yml
@@ -1,20 +1,20 @@
-name: metrics
-
-networks:
-  proxy_net:
-    external: true
-
-services:
-  glances:
-    image: nicolargo/glances:latest
-    container_name: glances
-    restart: unless-stopped
-    pid: host
-    environment:
-      - TZ=${TZ}
-      - GLANCES_OPT=-w
-    volumes:
-      - /var/run/docker.sock:/var/run/docker.sock:ro
-      - /etc/os-release:/etc/os-release:ro
-    networks:
+name: metrics
+
+networks:
+  proxy_net:
+    external: true
+
+services:
+  glances:
+    image: nicolargo/glances:latest
+    container_name: glances
+    restart: unless-stopped
+    pid: host
+    environment:
+      - TZ=${TZ}
+      - GLANCES_OPT=-w
+    volumes:
+      - /var/run/docker.sock:/var/run/docker.sock:ro
+      - /etc/os-release:/etc/os-release:ro
+    networks:
       - proxy_net
\ No newline at end of file
diff --git a/stacks/media/automation/docker-compose.yml b/stacks/media/automation/docker-compose.yml
index 6bc0a15..272194d 100644
--- a/stacks/media/automation/docker-compose.yml
+++ b/stacks/media/automation/docker-compose.yml
@@ -1,86 +1,86 @@
-name: arr
-
-networks:
-  media_net:
-    external: true
-  proxy_net:
-    external: true
-
-services:
-  prowlarr:
-    image: ghcr.io/linuxserver/prowlarr:latest
-    container_name: prowlarr
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-    volumes:
-      - ${DOCKER_ROOT}/arr/prowlarr:/config
-    networks:
-      - media_net
-      - proxy_net
-    restart: unless-stopped
-
-  radarr:
-    image: ghcr.io/linuxserver/radarr:latest
-    container_name: radarr
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-      - UMASK=022
-    volumes:
-      - ${DOCKER_ROOT}/arr/radarr:/config
-      - ${MEDIA}:/media
-      - ${DOWNLOADS}:/downloads
-    networks:
-      - media_net
-      - proxy_net
-    restart: unless-stopped
-
-  sonarr:
-    image: ghcr.io/linuxserver/sonarr:latest
-    container_name: sonarr
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-      - UMASK=022
-    volumes:
-      - ${DOCKER_ROOT}/arr/sonarr:/config
-      - ${MEDIA}:/media
-      - ${DOWNLOADS}:/downloads
-    networks:
-      - media_net
-      - proxy_net
-    restart: unless-stopped
-
-  mylar3:
-    image: ghcr.io/linuxserver/mylar3:latest
-    container_name: mylar3
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-    volumes:
-      - ${DOCKER_ROOT}/arr/mylar3:/config
-      - ${MEDIA}/manga:/manga
-      - ${DOWNLOADS}:/downloads
-    networks:
-      - media_net
-      - proxy_net
-    restart: unless-stopped
-
-  flaresolverr:
-    image: ghcr.io/flaresolverr/flaresolverr:latest
-    container_name: flaresolverr
-    environment:
-      - LOG_LEVEL=${LOG_LEVEL:-info}
-      - LOG_HTML=${LOG_HTML:-false}
-      # - CAPTCHA_SOLVER=${CAPTCHA_SOLVER:-none}
-      - TZ=${TZ}
-    ports:
-      - "127.0.0.1:8191:8191"
-    networks:
-      - media_net
+name: arr
+
+networks:
+  media_net:
+    external: true
+  proxy_net:
+    external: true
+
+services:
+  prowlarr:
+    image: ghcr.io/linuxserver/prowlarr:latest
+    container_name: prowlarr
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+    volumes:
+      - ${DOCKER_ROOT}/arr/prowlarr:/config
+    networks:
+      - media_net
+      - proxy_net
+    restart: unless-stopped
+
+  radarr:
+    image: ghcr.io/linuxserver/radarr:latest
+    container_name: radarr
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+      - UMASK=022
+    volumes:
+      - ${DOCKER_ROOT}/arr/radarr:/config
+      - ${MEDIA}:/media
+      - ${DOWNLOADS}:/downloads
+    networks:
+      - media_net
+      - proxy_net
+    restart: unless-stopped
+
+  sonarr:
+    image: ghcr.io/linuxserver/sonarr:latest
+    container_name: sonarr
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+      - UMASK=022
+    volumes:
+      - ${DOCKER_ROOT}/arr/sonarr:/config
+      - ${MEDIA}:/media
+      - ${DOWNLOADS}:/downloads
+    networks:
+      - media_net
+      - proxy_net
+    restart: unless-stopped
+
+  mylar3:
+    image: ghcr.io/linuxserver/mylar3:latest
+    container_name: mylar3
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+    volumes:
+      - ${DOCKER_ROOT}/arr/mylar3:/config
+      - ${MEDIA}/manga:/manga
+      - ${DOWNLOADS}:/downloads
+    networks:
+      - media_net
+      - proxy_net
+    restart: unless-stopped
+
+  flaresolverr:
+    image: ghcr.io/flaresolverr/flaresolverr:latest
+    container_name: flaresolverr
+    environment:
+      - LOG_LEVEL=${LOG_LEVEL:-info}
+      - LOG_HTML=${LOG_HTML:-false}
+      # - CAPTCHA_SOLVER=${CAPTCHA_SOLVER:-none}
+      - TZ=${TZ}
+    ports:
+      - "127.0.0.1:8191:8191"
+    networks:
+      - media_net
     restart: unless-stopped
\ No newline at end of file
diff --git a/stacks/media/downloads/docker-compose.yml b/stacks/media/downloads/docker-compose.yml
index 395967d..3d10ce3 100644
--- a/stacks/media/downloads/docker-compose.yml
+++ b/stacks/media/downloads/docker-compose.yml
@@ -1,45 +1,45 @@
-name: downloaders
-
-networks:
-  media_net:
-    external: true
-  proxy_net:
-    external: true
-
-services:
-  transmission:
-    image: lscr.io/linuxserver/transmission:latest
-    container_name: transmission
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-    volumes:
-      - ${DOCKER_ROOT}/downloaders/transmission:/config
-      - ${DOWNLOADS}:/downloads
-    ports:
-      - 9091:9091
-      - 51413:51413
-      - 51413:51413/udp
-    networks:
-      - media_net
-    restart: unless-stopped
-
-  deluge:
-    image: lscr.io/linuxserver/deluge:latest
-    container_name: deluge
-    environment:
-      - PUID=${PUID}
-      - PGID=${PGID}
-      - TZ=${TZ}
-      - DELUGE_LOGLEVEL=error
-    volumes:
-      - ${DOCKER_ROOT}/downloaders/deluge:/config
-      - ${DOWNLOADS}:/downloads
-    ports:
-      - 8112:8112
-      - 6881:6881
-      - 6881:6881/udp
-    networks:
-      - media_net
+name: downloaders
+
+networks:
+  media_net:
+    external: true
+  proxy_net:
+    external: true
+
+services:
+  transmission:
+    image: lscr.io/linuxserver/transmission:latest
+    container_name: transmission
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+    volumes:
+      - ${DOCKER_ROOT}/downloaders/transmission:/config
+      - ${DOWNLOADS}:/downloads
+    ports:
+      - 9091:9091
+      - 51413:51413
+      - 51413:51413/udp
+    networks:
+      - media_net
+    restart: unless-stopped
+
+  deluge:
+    image: lscr.io/linuxserver/deluge:latest
+    container_name: deluge
+    environment:
+      - PUID=${PUID}
+      - PGID=${PGID}
+      - TZ=${TZ}
+      - DELUGE_LOGLEVEL=error
+    volumes:
+      - ${DOCKER_ROOT}/downloaders/deluge:/config
+      - ${DOWNLOADS}:/downloads
+    ports:
+      - 8112:8112
+      - 6881:6881
+      - 6881:6881/udp
+    networks:
+      - media_net
     restart: unless-stopped
\ No newline at end of file
diff --git a/stacks/media/plex/docker-compose.yml b/stacks/media/plex/docker-compose.yml
index d9dad5b..32dbb63 100644
--- a/stacks/media/plex/docker-compose.yml
+++ b/stacks/media/plex/docker-compose.yml
@@ -1,25 +1,25 @@
-name: plex
-
-networks:
-  media_net:
-    external: true
-  proxy_net:
-    external: true
-
-services:
-  plex:
-    container_name: plex
-    image: ghcr.io/hotio/plex:latest
-    restart: unless-stopped
-    network_mode: host
-    environment:
-      PUID: ${PUID}
-      PGID: ${PGID}
-      TZ: ${TZ}
-      UMASK: 002
-    volumes:
-      - ${DOCKER_ROOT}/plex:/config
-      - ${MEDIA}:/plex
-      - /tmp:/transcode
-    devices:
+name: plex
+
+networks:
+  media_net:
+    external: true
+  proxy_net:
+    external: true
+
+services:
+  plex:
+    container_name: plex
+    image: ghcr.io/hotio/plex:latest
+    restart: unless-stopped
+    network_mode: host
+    environment:
+      PUID: ${PUID}
+      PGID: ${PGID}
+      TZ: ${TZ}
+      UMASK: 002
+    volumes:
+      - ${DOCKER_ROOT}/plex:/config
+      - ${MEDIA}:/plex
+      - /tmp:/transcode
+    devices:
       - /dev/dri:/dev/dri
\ No newline at end of file
diff --git a/stacks/voice/docker-compose.yml b/stacks/voice/docker-compose.yml
index 8816494..d923f91 100644
--- a/stacks/voice/docker-compose.yml
+++ b/stacks/voice/docker-compose.yml
@@ -1,24 +1,24 @@
-name: voice
-
-version: "3.8"
-
-services:
-  mumble:
-    image: mumblevoip/mumble-server:latest
-    container_name: murmur
-    restart: unless-stopped
-
-    ports:
-      - "64738:64738"
-      - "64738:64738/udp"
-
-    volumes:
-      - murmur-data:/data
-
-    environment:
-      - TZ=${TZ}
-      - MUMBLE_SUPERUSER_PASSWORD=${MUMBLE_SUPERUSER_PASSWORD}
-      - MUMBLE_SERVER_PASSWORD=${MUMBLE_SERVER_PASSWORD}
-      - MUMBLE_BANDWIDTH=72000
-volumes:
-  murmur-data:
+name: voice
+
+version: "3.8"
+
+services:
+  mumble:
+    image: mumblevoip/mumble-server:latest
+    container_name: murmur
+    restart: unless-stopped
+
+    ports:
+      - "64738:64738"
+      - "64738:64738/udp"
+
+    volumes:
+      - murmur-data:/data
+
+    environment:
+      - TZ=${TZ}
+      - MUMBLE_SUPERUSER_PASSWORD=${MUMBLE_SUPERUSER_PASSWORD}
+      - MUMBLE_SERVER_PASSWORD=${MUMBLE_SERVER_PASSWORD}
+      - MUMBLE_BANDWIDTH=72000
+volumes:
+  murmur-data:
